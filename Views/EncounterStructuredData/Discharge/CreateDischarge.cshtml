@model IS_Proj_HIT.ViewModels.EncounterStructuredDataViewModel
@{
    ViewBag.Title = "Create Discharge";
}

<h1>Create</h1>

<h4>Discharge</h4>
<hr />

<h3 class="error">@ViewData["ErrorMessage"]</h3>

<div class="row">
    <div class="col-md-10">
        <form method="post">
            @Html.AntiForgeryToken()
            <div class="form-row">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group col-sm-2">
                    <label asp-for="Discharge.WiPopCode" class="control-label">WiPopCode</label>
                    <input asp-for="Discharge.WiPopCode" 
                        class="form-control" maxlength="4"
                        placeholder="Example:  40" />
                    <span asp-validation-for="Discharge.WiPopCode" class="text-danger"></span>
                </div>
                <div class="form-group col-sm-4">
                    <label asp-for="Discharge.Name" class="control-label">Discharge Name</label>
                    <input asp-for="Discharge.Name" 
                        class="form-control" maxlength="200" required 
                        placeholder="Example:  Elective"/>
                    <span asp-validation-for="Discharge.Name" class="text-danger"></span>
                </div>
                <div class="form-group col-sm-6">
                    <label asp-for="Discharge.Description" class="control-label">Discharge Description</label>
                    <input asp-for="Discharge.Description" 
                        class="form-control"   
                        placeholder="Example:  The patient's condition allows time..."/>
                    <span asp-validation-for="Discharge.Description" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>

            </div>
        </form>
    </div>
</div>


<div>
    <a asp-controller="EncounterStructuredData" asp-action="ViewDischarges">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}