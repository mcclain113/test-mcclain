@model IS_Proj_HIT.ViewModels.PatientVm.PatientStructuredDataViewModel
@{
    ViewBag.Title = "Delete Legal Status";
}

<h1>Delete</h1>

<h3>@ViewData["RegularMessage"]</h3>
<h3 class="error">@ViewData["ErrorMessage"]</h3>

<h4>Legal Status</h4>
<hr />

<div class="row">
    <div class="col-md-10">
        <form method="post">
            @Html.AntiForgeryToken()
            <div class="form-row">

                <input type="hidden" asp-for="LegalStatus.LegalStatusId" value="@Model.LegalStatus.LegalStatusId" readonly/>

                <div class="form-group col-sm-4">
                    <label asp-for="LegalStatus.LegalStatusName" class="control-label">Legal Status Name</label>
                    <input asp-for="LegalStatus.LegalStatusName" 
                        class="form-control" maxlength="30" />
                </div>
            </div>
                
            <div class="form-row">
               <div class="form-group">
                    <a asp-controller="PatientStructuredData" asp-action="ViewLegalStatuses" class="btn btn-outline-danger">Discard Changes</a>
                </div>
                <div class="form-group">
                    <input type="submit" value="Delete Legal Status" class="ml-3 btn btn-primary" />
                </div>
            </div>
        </form>
    </div>
</div>