@model List<IS_Proj_HIT.Entities.Country>

@{
    ViewData["Title"] = "Countries";
}

<div class="row my-3">
    <h3>
        <a class="btn btn-primary" asp-controller="PatientStructuredData" asp-action="Index">
            <i class="fa fa-arrow-left "></i>
        </a>
        Countries
    </h3>
</div>

<p>
    <a class="btn btn-primary" asp-controller="PatientStructuredData" asp-action="CreateCountry">Create New</a>
</p>

<table class="stripe nowrap" id="countryTable">
    <thead>
        <tr>
            <th>
                Name
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var country in Model)
        {
            <tr>
                <td>
                    @country.Name
                </td>
                

                <td>
                   
                    <a class="btn btn-primary" asp-controller="PatientStructuredData" asp-action="EditCountry" asp-route-id="@country.CountryId">Edit</a>
                    <a class="btn btn-danger" asp-controller="PatientStructuredData" asp-action="DeleteCountry" asp-route-id="@country.CountryId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section scripts{
    <script>
        // Creates DataTable
        // DataTable Documentation found here: https://datatables.net/
        $(function () {
            $('#countryTable').DataTable({
                columns: [null, { orderable: false }],
                order: [[0, 'asc']],
                pageLength: 10,
                searching: true,
                language: {
                    emptyTable: 'None found'
                }
            });
        });
    </script>
}