@model IS_Proj_HIT.ViewModels.DisclosureStructuredData.DisclosureStructuredDataViewModel
@{
    ViewBag.Title = "Create Request Status";
}

<h1>Create</h1>

<h4>Request Status</h4>
<hr />

<div class="row">
    <div class="col-md-10">
        <form method="post">
            @Html.AntiForgeryToken()
            <div class="form-row">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
               <div class="form-group col-sm-2">
                    <label asp-for="RequestStatus.RequestStatusId" class="control-label">Status Id</label>
                    <input asp-for="RequestStatus.RequestStatusId" 
                        class="form-control" maxlength="2" required
                        placeholder="Example:  CP" />
                    <span asp-validation-for="RequestStatus.RequestStatusId" class="text-danger"></span>
                </div>
                <div class="form-group col-sm-4">
                    <label asp-for="RequestStatus.RequestStatus1" class="control-label">Name of Status</label>
                    <input asp-for="RequestStatus.RequestStatus1" 
                        class="form-control" maxlength="20" required
                        placeholder="Example:  Completed" />
                    <span asp-validation-for="RequestStatus.RequestStatus1" class="text-danger"></span>
                </div>
                <div class="form-group col-sm-6">
                    <label asp-for="RequestStatus.StatusDescription" class="control-label">Description of Status</label>
                    <input asp-for="RequestStatus.StatusDescription" 
                        class="form-control" maxlength="100"  
                        placeholder="Example:  The request is completed"/>
                    <span asp-validation-for="RequestStatus.StatusDescription" class="text-danger"></span>
                </div>
                                                
                @if (ViewBag.ErrorMessage != null) {
                    <div class="form-group col-12">
                        <span class="text-danger">@ViewBag.ErrorMessage</span>
                    </div>
                }


                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>

            </div>
        </form>
    </div>
</div>


<div>
    <a asp-controller="DisclosureStructuredData" asp-action="ViewRequestStatuses">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}