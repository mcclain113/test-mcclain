@model IS_Proj_HIT.ViewModels.PhysicianStructuredData.PhysicianStructuredDataViewModel
@{
    ViewBag.Title = "Create Provider Type";
}

<h1>Create</h1>

<h4>Provider Type</h4>
<hr />

<div class="row">
    <div class="col-md-10">
        <form method="post">
            @Html.AntiForgeryToken()
            <div class="form-row">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                
                <div class="form-group col-sm-6">
                    <label asp-for="ProviderType.Name" class="control-label">Name of Provider Type</label>
                    <input asp-for="ProviderType.Name" 
                        class="form-control" maxlength="100" required
                        placeholder="Example:  Physician Assistant" />
                    <span asp-validation-for="ProviderType.Name" class="text-danger"></span>
                </div>
                <div class="form-group col-sm-6">
                    <label asp-for="ProviderType.Description" class="control-label">Description</label>
                    <input asp-for="ProviderType.Description" 
                        class="form-control" maxlength="100" 
                        placeholder="Example:  This field is optional"/>
                    <span asp-validation-for="ProviderType.Description" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>

            </div>
        </form>
    </div>
</div>


<div>
    <a asp-controller="PhysicianStructuredData" asp-action="ViewProviderTypes">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}