@model IS_Proj_HIT.ViewModels.PcaStructuredDataViewModel
@{
    ViewBag.Title = "Edit Pain Rating";
    var userPermissions = ViewData["UserPermissions"] as List<string>;
}

<h1>Edit</h1>

<h4>Pain Rating</h4>
<hr />

<div class="row">
    <div class="col-md-10">
        <form method="post">
            @Html.AntiForgeryToken()
            <div class="form-row">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="PainRating.PainRatingId" value="@Model.PainRating.PainRatingId" readonly/>
                <div class="form-group col-sm-6">
                    <label asp-for="PainRating.Value" class="control-label">Care System Parameter Name</label>
                    <input asp-for="PainRating.Value" 
                        class="form-control" required type="number" />
                    <span asp-validation-for="PainRating.Value" class="text-danger"></span>
                </div>
                <div class="form-group col-sm-6">
                    <label asp-for="PainRating.PainParameterId" class="control-label">Select Care System Type</label>
                    <select asp-for="PainRating.PainParameterId" class="form-control" asp-items="ViewBag.PainParameterId" required data-val-required="A Care System Type selection is required"></select>
                    <span asp-validation-for="PainRating.PainParameterId" class="text-danger"></span>
                </div>
            </div>
            <div class="form-row">
                <div class="form-group col-12">
                    <label asp-for="PainRating.Description" class="control-label">Enter Normal Limits Description</label>
                    <textarea asp-for="PainRating.Description" class="form-control" rows="4" placeholder="Enter description here..."></textarea>
                    <span asp-validation-for="PainRating.Description" class="text-danger"></span>
                </div>
            </div>
            <div class="form-row">
                <div class="form-group">
                    <a asp-controller="PcaStructuredData" asp-action="ViewPainRatings" class="btn btn-outline-danger">Discard Changes</a>
                </div>           
                <div class="form-group ml-3">
                    @if (userPermissions != null && (userPermissions.Contains("StructuredDataEdit")) )
                    {
                        <input type="submit" value="Save Changes" class="btn btn-primary" />
                    }
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}