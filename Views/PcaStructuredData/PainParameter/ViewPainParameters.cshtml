@model List<IS_Proj_HIT.Entities.PainParameter>

@{
    ViewData["Title"] = "Pain Parameters";
    var userPermissions = ViewData["UserPermissions"] as List<string>;
}

<div class="row my-3">
    <h3>
        <a class="btn btn-primary" asp-controller="PCAStructuredData" asp-action="Index">
            <i class="fa fa-arrow-left "></i>
        </a>
        Pain Parameters
    </h3>
</div>
<!-- banner displays used when deleting an item  -->
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">
            @TempData["SuccessMessage"]
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">
            @TempData["ErrorMessage"]
        </div>
    }
    
<div>
    @if (userPermissions != null && (userPermissions.Contains("StructuredDataAdd")) )
    {
        <a class="btn btn-primary mb-3" asp-controller="PCAStructuredData" asp-action="CreatePainParameter">Create New</a>
    }
</div>

<table class="stripe nowrap" id="painParameterTable">
    <thead>
        <tr>
            <th>Pain Parameter Name</th>
            <th>Description</th>
            <th>Pain Scale Type</th>
            <th style="text-align: center;">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var painParameter in Model)
        {
            <tr>
                <td>@painParameter.ParameterName</td>
                <td>@painParameter.Description</td>
                <td>@painParameter.PainScaleType.PainScaleTypeName</td>
                <td>
                    @if (userPermissions != null && (userPermissions.Contains("StructuredDataEdit")) )
                    {
                        <a class="btn btn-primary" asp-controller="PCAStructuredData" asp-action="EditPainParameter" asp-route-id="@painParameter.PainParameterId">Edit</a>
                    }
                    @if (userPermissions != null && (userPermissions.Contains("StructuredDataDelete")) )
                    {
                        <a class="btn btn-danger" onclick="return confirm('Are you sure?')" asp-controller="PCAStructuredData" asp-action="DeletePainParameter" asp-route-id="@painParameter.PainParameterId">Delete</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
<style>
    /* Ensure the entire table fits within the view screen */
    #painParameterTable {
        table-layout: auto;
        width: 100%; /* Adjusts the table width to fit the viewport */
    }

    /* Apply text wrapping to the second column */
    #painParameterTable td:nth-child(2) {
        white-space: normal; /* Allows text to wrap within the cell */
        word-wrap: break-word; /* Break long words if necessary */
    }
</style>
@section scripts{
    <script>
        // DataTable Documentation found here: https://datatables.net/
        $(document).ready(function () {
            // Creates DataTable
            $('#painParameterTable').DataTable({
                columns: [null,null,null, { orderable: false }],
                ordering: true,
                order: [],
                pageLength: 10,
                searching: true,
                language: {
                    emptyTable: 'None found'
                }
            });
        });
    </script>
}